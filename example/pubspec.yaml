name: multi_logger_example
description: An example of using multi_logger package
publish_to: none
version: 0.0.12

environment:
  sdk: ^3.10.0

dependencies:
  flutter:
    sdk: flutter
  multi_logger: any
  sentry: any

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
